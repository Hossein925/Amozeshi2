# راهنمای مدیریت محتوا به صورت دستی

شما می‌توانید محتوای سامانه (بخش‌ها، بیماری‌ها و فایل‌های ضمیمه) را با ویرایش فایل‌های موجود در این پوشه (`data`) مدیریت کنید.

## ساختار پوشه‌ها

- هر پوشه در مسیر `data/` معرف یک **بخش** در برنامه است (مانند `emergency`).
- درون هر پوشه بخش، یک پوشه برای هر **بیماری** وجود دارد (مانند `acute-myocardial-infarction`).

---

### چگونه یک بخش جدید اضافه کنیم؟

1.  یک پوشه جدید با نام انگلیسی (ID) در مسیر `data/` بسازید. مثال: `data/new-section`.
2.  فایل `data/sections.json` را باز کرده و یک آبجکت جدید به آرایه آن اضافه کنید. مثال:
    ```json
    {
      "id": "new-section",
      "name": "نام بخش جدید",
      "icon": "🆕"
    }
    ```
3.  درون پوشه `data/new-section/` یک فایل به نام `diseases.json` با محتوای یک آرایه خالی `[]` بسازید.
4.  حالا می‌توانید طبق دستورالعمل زیر، بیماری‌های جدید به این بخش اضافه کنید.

---

### چگونه یک بیماری جدید اضافه کنیم؟

1.  به پوشه بخش مورد نظر بروید (مثال: `data/emergency/`).
2.  یک پوشه جدید با نام انگلیسی (ID) برای بیماری خود بسازید. مثال: `data/emergency/new-disease`.
3.  فایل `diseases.json` همان بخش (`data/emergency/diseases.json`) را باز کرده و ID بیماری جدید را به آرایه اضافه کنید. مثال: `"new-disease"`.
4.  درون پوشه بیماری جدید (`data/emergency/new-disease/`) دو فایل بسازید:
    *   `description.txt`: حاوی توضیحات کامل بیماری.
    *   `manifest.json`: حاوی نام بیماری و لیست فایل‌های ضمیمه. مثال:
        ```json
        {
          "name": "نام بیماری جدید",
          "files": []
        }
        ```

---

### چگونه یک فایل ضمیمه (PDF, عکس, صوت) اضافه کنیم؟

1.  فایل خود را (مثلا `guide.pdf`) در پوشه بیماری مورد نظر کپی کنید (مثال: `data/emergency/acute-myocardial-infarction/`).
2.  فایل `manifest.json` همان پوشه را باز کنید.
3.  یک آبجکت جدید به آرایه `files` اضافه کنید.

    - **`path`**: نام دقیق فایل شما (مثال: `"guide.pdf"`).
    - **`name`**: نامی که در برنامه نمایش داده می‌شود (مثال: `"راهنمای PDF"`).
    - **`description`**: توضیحی کوتاه درباره فایل.
    - **`type`**: نوع فایل. مقادیر مجاز: `"pdf"`, `"image"`, `"audio"`, `"unknown"`.

    **مثال برای `manifest.json`:**
    ```json
    {
      "name": "سکته حاد قلبی",
      "files": [
        {
          "path": "guide.pdf",
          "name": "راهنمای PDF برای بیماران",
          "description": "نکات مهم پس از سکته قلبی.",
          "type": "pdf"
        },
        {
          "path": "heart-sound.mp3",
          "name": "صدای قلب",
          "description": "نمونه صدای قلب سالم.",
          "type": "audio"
        }
      ]
    }
    ```

---

### چگونه بنرهای صفحه اصلی را مدیریت کنیم؟

1.  فایل تصویر بنر خود را در پوشه `data/banners/` قرار دهید.
2.  فایل `data/banners.json` را باز کنید.
3.  یک آبجکت جدید به آرایه اضافه کنید. `imageUrl` باید مسیر فایل تصویری باشد که در مرحله 1 اضافه کردید.

    **مثال برای `banners.json`:**
    ```json
    [
      {
        "id": "new-banner-id",
        "title": "عنوان بنر جدید",
        "description": "توضیحات بنر جدید.",
        "imageUrl": "./data/banners/new-image.jpg"
      }
    ]
    ```

**نکته مهم:** پس از اعمال تغییرات، صفحه برنامه را رفرش کنید تا محتوای جدید بارگذاری شود.